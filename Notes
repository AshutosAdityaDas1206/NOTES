//CLASS 1:

Loops:
1)Entry based(While,for)
2)Exit based(do-while)

\\Q.Use while loop to take inputs inside the loop check if its a natural no and keep looping until the number is natural .
#include <stdio.h>
int main()
{
    int a;
    // printf("Enter a number to check:\n");
    // scanf("%d",&a);
    while (2 > 1)
    {
        printf("Enter a number to check:\n");
        scanf("%d", &a);
        if (a > 0)
        {
            printf("Natural!");
            break;
        }
        else
        {
            printf("Repeat\n");
        }
    }

    return 0;
}

//NOTE;Break; not only forces program control to exit the switchcase,but it also breaks the loops in which it is contained.

//Q2.Code for calculating factorial of user input number:

#include <stdio.h>
int main()
{
    int num, fact;

    printf("Enter a number:\n");
    scanf("%d", &num);
    fact = 1;
    for (int i = 1; i <= num; i++)
    {
        fact = fact * i;
    }
    printf("%d", fact);

    return 0;
}


//for making a for loop finite,ALL THREE CONDITIONS ARE REQUIRED!(otherwise it will run infinitely)

//NOTE:Application of Blank/Empty loops are to create time delay whenever required in our program.
//ex: for (int i = 0;i<=5;i++);

//here , the ';' symbol terminates the for loop and doesnt allows the code to run

//or (int i = 0;i<=5;i++){} //used to delay time.
//----------------------------------------------------------------------------------------------------------------------------------------------------------------------

//ALL TYPES OF ERRORS:
//1)Syntax Error:Grammatical mistakes identifed by compilations
//2)Runtime Error: Encountered during running a program
//3)Logical Error:Fallacy error like grabage values,unwanted output,Dividing by zero.outputs provide bogus results.

CLASS 2:
Loops:
q1)use nested loops to find factorial of first 10 natural numbers
q2)use while loop
q3)wap to reverse a number usinf while loop
q4)wap to find sum of digits of a number
q5)wap to check if a no. is an armstrong no.
q6)wap to check for perfect no.
----------------------------------------------------------------------------------------------------------------------------------------------------------------------
Q.Where is for used instead of while?
Q.When can we use both ?
Q.Demonstrate the usage of break with flowchart//in loop
Q.Same for continue.//in loop


---------------------------------------------------------------------------------------------------------------------------------------------------------------------------
Do-While:
1.Exit controlled
2.It will run at least once
3.Exiting the loop depends on the condition.

---------------------------------------------------------------------------------------------------------------------------------------------------------------------
CLASS 4:
Fnuction:
specialized set of instructoin used to solve a subtask.
(in code) a block of statements with the specific purpose of solving a problem with a same piece of code.


-Library defined function:
already defined in the language ,used by including header files.
-User defined functions:
added by programmers in their programs.

Syntax:
1)Declaration/prototype/header
2)Definition
3)Call


















